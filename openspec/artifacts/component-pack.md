# Component Pack

Selected portfolio pack: `mlops-data-platform` with `kafka-streams` language/decision profile.

Consumed:

- benchmark-result-v2 contract;
- Gradle/JVM wrapper validator;
- Kafka Streams skill and decision matrix;
- continuity checkpoint;
- OpenSpec/AITmpl governance files.

No UI design-system runtime is installed because the repository has no UI. Shared README/diagram conventions still apply.
