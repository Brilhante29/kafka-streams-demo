# Archived benchmark evidence

`topology-driver-legacy-2026-07-18.json` is preserved for historical traceability only. It was produced by the old TopologyTestDriver harness with Kotlin 2.0.21/Kafka Streams 3.8.1 and 100,000 records.

It is not broker throughput, does not follow benchmark-result-v2, and must not be used as the current README number. The release baseline belongs at `benchmarks/results/baseline.json` and must come from the real Kafka broker harness with clean provenance.
