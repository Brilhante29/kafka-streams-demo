id: 28
name: kafka-streams-demo
program:
  id: mlops-data-platform
  narrative_fit: "Demonstra uma etapa de streaming reproduzÃƒÂ­vel para mover eventos de negÃƒÂ³cio a features agregadas com contrato e benchmark local."
  shared_assets:
    - streaming benchmark JSON
    - deterministic topology fixture
status: published
claim: processamento streaming
stack: [kotlin, java21, gradle, kafka-streams, junit5, docker, redpanda-optional]
language_profile:
  primary: spring-kotlin-backend
  secondary: []
  reason: "Kotlin/JVM e testes de infraestrutura sÃƒÂ£o o sinal pÃƒÂºblico pedido; a topologia permanece um app de linha de comando sem Spring para manter o nÃƒÂºcleo mÃƒÂ­nimo."
decision_brain:
  agentic_spec:
    source: .portfolio/decision-brain/agentic-spec-governance.yaml
    spec_store: portfolio-reuse-kit
    schema: portfolio-system
    project_config: openspec/config.yaml
    component_pack: mlops-data-platform
    artifact_graph:
      - intent->portfolio-impact->architecture-record
      - architecture-record+portfolio-impact->component-pack
      - component-pack->reuse-delta->benchmark-proof->tasks->verification
  agent_graph:
    mode: principal-agent-orchestrated
    source: .portfolio/decision-brain/agent-graph.yaml
    handoff: sdd/agent-handoff.md
    local_first_runtime: docker+topology-test-driver
  reuse_improvement_loop:
    source: .portfolio/decision-brain/reuse-improvement-loop.yaml
    review_path: sdd/reuse-improvement-review.md
    current_status: recorded
  principles:
    coupling_rule: domain/use cases do not depend on framework, database, broker, cloud SDK, transport, or UI
    solid: [srp, ocp, lsp, isp, dip]
    simplicity_rule: KISS/YAGNI; add infrastructure only when the claim needs it
    testability_rule: domain and topology execute through pure tests or TopologyTestDriver
    rejected_overengineering: [spring-http, mandatory-broker, database, microservices, cloud-sdk]
  stack_profile: spring-kotlin-backend
  api_style: cli
  messaging: kafka
  cloud:
    mode: adapter-fake
    local_provider: kumo
    real_provider_target: aws
    emulated_services: []
    adapter_rule: "Sem integraÃƒÂ§ÃƒÂ£o cloud neste escopo; futuras portas mantÃƒÂªm SDK fora do domÃƒÂ­nio."
    config_switch: CLOUD_PROVIDER=kumo|aws|none
    unsupported_behaviors: ["Kumo nÃƒÂ£o ÃƒÂ© necessÃƒÂ¡rio sem serviÃƒÂ§o AWS usado pela topologia"]
  database: none
  runtime: "JVM 21, TopologyTestDriver no default; KafkaStreams.run via adapter explÃƒÂ­cito"
  library_policy: "Kafka Streams e test-utils provam a semÃƒÂ¢ntica; Kotlin serialization mantÃƒÂ©m o contrato JSON pequeno; Ktlint fecha o gate de estilo."
  record_path: sdd/technical-decision.md
  rejected_options:
    - option: redpanda-no-default
      reason: "Broker nÃƒÂ£o ÃƒÂ© necessÃƒÂ¡rio para provar join/agregaÃƒÂ§ÃƒÂ£o e tornaria testes e benchmark dependentes de infraestrutura."
architecture:
  style: event-driven
  reason: "Eventos, stream/table join, estado agregado e throughput da topologia sÃƒÂ£o o problema e a mÃƒÂ©trica."
  problem_forces:
    domain_complexity: low
    integration_pressure: high
    ui_state_complexity: none
    data_or_ml_reproducibility: high
    auditability: medium
    throughput_or_async: high
    independent_deployability: low
  dependency_rule: adapters depend inward; domain and policy do not import Kafka, cloud, Docker, or transport
  boundaries:
    - "domain nÃƒÂ£o importa Kafka, Docker ou cloud"
    - "infra constrÃƒÂ³i a topologia e os adaptadores de runtime"
    - "benchmark usa o mesmo contrato atravÃƒÂ©s do TopologyTestDriver"
  rejected_alternatives:
    - style: microservices
      reason: "Um ÃƒÂºnico fluxo nÃƒÂ£o exige deploy independente; dividir sÃƒÂ³ adicionaria custo distribuÃƒÂ­do."
benchmark:
  primary_metric: messages_per_second
  unit: messages/s
  command: gradle run --args="benchmark 1000 benchmarks/results/latest.json"
  result_path: benchmarks/results/baseline.json
release:
  docker: "docker build -t kafka-streams-demo . && docker run --rm kafka-streams-demo"
  no_secret_default_path: true
  readme_has_number: true
  references_documented: true
